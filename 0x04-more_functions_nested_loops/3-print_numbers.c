#include "main.h"
/**
 * print_numbers - Prints numbers from 0 to 9
 * Return: 0 (Success)
 */
void print_numbers(void)
{
	int x;

	if (x >= 0 && x <= 9)
	{
	_putchar(x);
	_putchar('\n');
	}
	return (0);
}

